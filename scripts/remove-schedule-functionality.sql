-- Remove schedule table and related data
DROP TABLE IF EXISTS schedule CASCADE;

-- Remove any schedule-related permissions or references
-- (This is a cleanup script to remove schedule functionality)
